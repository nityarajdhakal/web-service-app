# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=miniapp_db
DB_USER=miniapp_user
DB_PASSWORD=miniapp_password_change_this

# JWT Secret (Change this to a random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Backend Port
PORT=5000

# Node Environment
NODE_ENV=production

