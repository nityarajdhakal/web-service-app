# Backend Environment Variables

# Server
PORT=5000
NODE_ENV=development

# Database - Local development (PostgreSQL)
DB_USER=postgres
DB_HOST=localhost
DB_DATABASE=mini_app
DB_PASSWORD=your_password
DB_PORT=5433

# Database - Production (Render PostgreSQL)
# Format: postgresql://user:password@host:port/database
# Get this from your Render PostgreSQL service dashboard
DATABASE_URL=postgresql://user:password@your-render-host.postgres.render.com:5432/your_db

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_secret_key_here

# CORS Configuration
# Set to your frontend URL. Examples:
# - Local: http://localhost:3000 or http://localhost:5173
# - Production: https://your-frontend.vercel.app
CORS_ORIGIN=https://web-service-application.onrender.com

# Rate Limiting
RATE_LIMIT_MAX=120

# Frontend Distribution Path (optional, for serving built frontend)
# FRONTEND_DIST_PATH=/path/to/miniapp-frontend/dist
